type Review {
  id: ID!
  profileId: ID!
  movieId: ID
  episodeId: ID
  rating: Int!
  comment: String
  createdAt: String!
}

input CreateReviewInput {
  profileId: ID!
  movieId: ID
  episodeId: ID
  rating: Int!
  comment: String
}

input UpdateReviewInput {
  rating: Int
  comment: String
}

extend type Query {
  getReview(id: ID!): Review @auth
  listReviews(profileId: ID!): [Review!]! @auth
}

extend type Mutation {
  createReview(input: CreateReviewInput!): Review! @auth @hasRole(role: MEMBER)
  updateReview(id: ID!, input: UpdateReviewInput!): Review! @auth @hasRole(role: MEMBER)
  deleteReview(id: ID!): Boolean! @auth @hasRole(role: MEMBER)
}
